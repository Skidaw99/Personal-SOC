/**
 * Layout Engine — panel IDs only.
 * All positioning is CSS-driven via state classes on the root element.
 * See index.css: .state-calm .panel-globe { ... }
 */

export const PANELS = {
  GLOBE:   'globe',
  FEED:    'feed',
  ACTOR:   'actor',
  INTEL:   'intel',
  COPILOT: 'copilot',
}
